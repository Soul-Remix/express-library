<h1>{{title}}</h1>

{{#if bookinstance.length}}
<p><strong>Delete the following book instance before attempting to delete this book.</strong></p>
<div>
    <h2>Book Instance</h2>
    <ul>
        {{#each bookinstance}}
        <li><a href={{this.url}}>{{this.imprint}}</a></li>
        {{/each}}
    </ul>
</div>

{{else}}
<p>Do you really want to delete this Book</p>
<form action="" method="POST">
    <input type="hidden" name='bookid' required value={{book._id}}>
    <button type="submit">Delete</button>
</form>
{{/if}}