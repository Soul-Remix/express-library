<h1>{{title}}</h1>
{{#if data}}
<ul>
    {{#each data}}
    <li><a href="{{this.url}}">{{this.name}}</a>
        <p>{{this.date_of_birth}} - {{this.date_of_death}}</p>
    </li>
    {{/each}}
</ul>
{{else}}
<div>There are no authors.</div>
{{/if}}