<h1>{{title}}</h1>
{{#if data}}
<ul class="bookinstance-ul">
    {{#each data}}
    <li><a href="{{this.url}}">{{this.book.title}}: {{this.imprint}}</a><span data-val="{{this.status}}">{{this.status}}</span></li>
    {{/each}}
</ul>
{{else}}
    <div>There are no book copies in this library.</div>
{{/if}}